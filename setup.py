from setuptools import setup, find_packages

setup(
    name="CornBreadV3",  # Replace with your project name
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here, e.g., 'numpy', 'pandas'
    ],
)
